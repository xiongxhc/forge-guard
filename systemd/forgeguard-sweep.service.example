[Unit]
Description=forge-guard protection sweep

[Service]
Type=oneshot
ExecStart=/bin/bash -lc "set -a && source %h/.config/forge-guard/forge-guard.env && set +a && cd __REPO__/forge-guard && exec .venv/bin/python -m forgeguard.cli sweep"
